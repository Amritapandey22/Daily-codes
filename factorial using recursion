#include<iostream>
using namespace std;

int factorial(int n)
{
    if(n==0)
    return 1;
    
    int fnm1=factorial(n-1); //Factorial of n-1
    
    return n*fnm1;
    
}


int main()
{
    int n; cin>>n;
    cout<<factorial(n);
}
